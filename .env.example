# Copy this to .env.local for local dev (usually not needed — Vite proxy handles it)
# Only required for production builds pointing to a deployed backend
VITE_API_BASE=https://your-backend.onrender.com
